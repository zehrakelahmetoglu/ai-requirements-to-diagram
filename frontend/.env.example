# Ortam Değişkenleri — AI Requirements to Diagram (Frontend)
# Bu dosyayı .env olarak kopyalayın ve değerleri güncelleyin:
#   cp .env.example .env

# Backend API adresi (varsayılan: yerel FastAPI)
VITE_API_URL=http://127.0.0.1:8000
